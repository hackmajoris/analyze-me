services:
  app:
    build: .
    volumes:
      - "${HOME}/Library/Mobile Documents/com~apple~CloudDocs/AnalyzeMe:/data"
    environment:
      DB_PATH: /data/blood_tests.db
      DB_KEY: ${DB_KEY}
    networks:
      - proxy

networks:
  proxy:
    external: true
